---
title: 實驗數據製圖
---

研究中

```vega
{
  "$schema": "https://vega.github.io/schema/vega-lite/v4.json",
  "data": {
    "url": "https://raw.githubusercontent.com/OowhitecatoO/LiBatteryLab/master/data/0.0276g0.087mm.D01",
    "format": {
      "type": "csv",
      "parse": { "LOOP": "number" }
    }
  },
  "transform": [
    {"calculate": "year(datum.Date)", "as": "year"},
    {"calculate": "floor(datum.year / 10)", "as": "decade"},
    {
      "calculate": "(datum.year % 10) + (month(datum.Date)/12)",
      "as": "scaled_date"
    },
    {
      "calculate": "datum.first_date === datum.scaled_date ? 'first' : datum.last_date === datum.scaled_date ? 'last' : null",
      "as": "end"
    }
  ],
  "mark": "line",
  "encoding": {
    "x": {
      "field": "LOOP",
      "type": "quantitative"
    },
    "y": {
      "field": "VOLTAGE",
      "type": "quantitative"
    }
  }
}
```
